---
import {Image} from 'astro:assets';
import heroImage from '../assets/hero.jpeg';
---
<!-- Hero Section -->
<section id="home" class="relative h-[80vh] flex items-center justify-center text-center pt-20 overflow-hidden">
   <!-- Background Video -->
   <Image src={heroImage} class="absolute inset-0 w-full h-full object-cover z-0" alt="Hero Image of Client that has her lashes done"/>
   <!-- Dark Overlay -->
   <div class="absolute inset-0 bg-black opacity-30"></div>
   <!-- Content -->
   <div class="relative z-20 max-w-3xl px-6 text-white">
      <h1 class="text-4xl md:text-6xl font-serif font-light mb-6 tracking-wider">Your Look. Your Lashes.</h1>
      <p class="text-lg md:text-xl mb-10 font-light max-w-2xl mx-auto">Maßgeschneidertes Lash- & Brow Design, präzises Permanent Make-up und hochwertige 1:1 Schulungen.</p>
      <a href="#services" class="inline-block px-8 py-3 border border-white text-white hover:bg-white hover:text-black transition duration-300">Jetzt unsere Leistungen entdecken</a>
   </div>
</section>